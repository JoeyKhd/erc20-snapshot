# RPC endpoint of the chain node (https:// or wss://)
RPC_URL=
# Block at which the token contract was deployed
DEPLOYMENT_BLOCK=
# Block to snapshot on: a block number or a tag (latest, earliest, pending, safe, finalized)
SNAPSHOT_BLOCK=
# Amount of blocks scanned per batch when collecting Transfer events
BLOCKS_PER_BATCH=
# Delay in milliseconds between batches to avoid rate limiting
SLEEP_TIMEOUT=
# Address of the ERC20 token contract
CONTRACTADDRESS=
